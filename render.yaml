services:
  - type: web
    name: readmitflow-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: DEMO_ONLY
        value: "true"
      - key: ALLOWED_ORIGINS
        value: "*"
